<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>

        <!-- Add these labels to make lazy loading is enabled -->
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="aggressiveLazyLoading" value="false"/>


    </settings>
    
    
    
    <environments default="mysql">
        <environment id="mysql">
            <transactionManager type="JDBC"/>
<!--
            The type property in dataSource label is the type of connect pool,
            this have three kinds type:
            1. pooled : <Build the connection pool> The connect pool that traditional rule in java.sql.DataSource
               mybatis have the implement

            2. unpooled : <Don't build the connection pool> have no pool, mybatis will get a new connection very get a session

            3. JNDI : use the configuration of the server build the pool,
               you only use it in war package in web or maven project
               such as in tomcat server, use dbcp pool.

            If have no special conditions, use pooled.
-->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://121.5.173.35:3306/judger"/>
                <property name="username" value="root"/>
                <property name="password" value="000826...MMHPP"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <package name="com.kalzn.mapper"/>
    </mappers>
</configuration>